# Charlottesville ingestion uses only the Python standard library (urllib, json).
# No third-party deps required to run charlottesville.py.
#
# Optional, for the richer pipeline (DB load, geo, validation) as we build it out:
# psycopg[binary]>=3.1     # write to Supabase/Postgres
# pydantic>=2.7            # validate/normalize records into the unified schema
# shapely>=2.0             # parcel geometry (when we add returnGeometry=true)
# httpx>=0.27              # async paged fetches across many layers/markets
