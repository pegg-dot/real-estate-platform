{
  "name": "lot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LOT — the judgment layer (scoring + creative-finance engines). The moat.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "refresh": "tsx scripts/refresh-market.ts",
    "dossier": "tsx scripts/refresh-market.ts --skip-ingest"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "postgres": "^3.4.9"
  }
}
